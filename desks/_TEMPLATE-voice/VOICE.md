# _TEMPLATE - the distilled rules

Short on purpose. This file is somebody's summary of a voice, so it drifts. `corpus/`
is the voice itself and does not. Read this second, after the samples.

## Sound like

- [One line per trait, concrete enough to point at in a sample.]
- [Say which sample it came from. A trait with no sample behind it is an adjective.]

## Never sound like

- [The list that does the work. It is far easier to write than the list above, because
  "not that" is observable and "warm but direct" is not.]
- [One line per thing. Add the phrase itself in backticks when it is a word.]

## How the never-list is enforced

Every backticked term in the section above is added to the linter's tell list by
`scripts/check-voice.py`, so an entry there is a check rather than a note. Write the
literal word or phrase in backticks when you want it caught; write prose when you do not.

## Registers inside this register

- [Which mediums this desk covers, and what changes between them.]
